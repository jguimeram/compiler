function fib($n) {
    if ($n == 0) {
        return 0;
    }
    if ($n == 1) {
        return 1;
    }
    return fib($n - 1) + fib($n - 2);
}

// Compute Fibonacci numbers 0–9
$i = 0;
while ($i < 10) {
    $value = fib($i);
    print($value);
    $i = $i + 1;
}

// Demonstrate if/else
$x = 7;
if ($x % 2 == 0) {
    print(100);
} else {
    print(101);
}

// Simple arithmetic
$a = 5 + 3 * 2;   // 11
$b = (5 + 3) * 2; // 16
print($a);
print($b);
